# Examples

English · 简体中文

`demo.py` walks the Alpha ledger (`farfield init` / `status` / `verify`). That is not the scientist loop. For a research mission use `python3 -m farfield research`.

`demo.py` 演示 Alpha 账本，不是 scientist 循环。研究任务请用 `farfield research`。
