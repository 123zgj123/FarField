---
name: kill-argument
description: Compile the strongest registered objection on a FarField card. Use when the human packet needs a kill argument, or before a refine that forgot the card already named a no.
stage: writeup, intern
admitted: true
audience: research
---

# Kill argument (FarField)

Adapted from ARIS `/kill-argument`. FarField does not spawn a hostile reviewer LLM and bank it as evidence.

Compiler: `render_kill_argument` → `ideas/<card_id>/idea-stage/RESEARCH_BRIEF.md` section 非目标, from `objection`, `falsifier`, `dead_end`.

An empty objection is not a pass. The next `idea_rounds` rewrite of this pair must write one that is not a restatement of the claim. Those fields never climb the ladder.
