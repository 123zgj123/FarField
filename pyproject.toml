[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "farfield-runtime"
version = "0.1.0"
description = "An experimental ScientificState-driven research runtime with trajectory-conditioned exploration and evidence-gated conclusions"
license = {text = "MIT"}
requires-python = ">=3.11"
dependencies = []

[project.scripts]
farfield = "farfield.cli:main"

[tool.setuptools]
package-dir = {"" = "src"}

[tool.setuptools.packages.find]
where = ["src"]
